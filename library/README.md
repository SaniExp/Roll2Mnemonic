# Archived code — do not execute

Everything under this directory is historical development material and is not part of the active application.

These snapshots include obsolete implementations, including insecure randomness and old entropy-generation behavior. Do not run any Python file from this directory and do not use any output from it for wallet creation.

Use only the root application (`main.py`) after reviewing the current source, pinned dependencies, and security warnings.
